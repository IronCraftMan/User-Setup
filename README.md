# User-Setup
Sets up new user accounts on OS X.

Uses a simple AppleScript and terminal commands. 

### Upcoming Changes:
- Setting Terminal Prefs
- Finishing Safari Prefs

#### Using Finder Prefs
You’ll need to change “com.apple.finder.plist” to your own preference (located in ~/Library/Preferences/), then compile.